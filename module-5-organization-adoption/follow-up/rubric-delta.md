# Rubric Delta Prompt

Ask participants to send three numbers in chat:

- `before -> after` for `Feedback signals`
- `before -> after` for `Context substrate`
- `before -> after` for `Blast radius control`

Then ask one precise self-question:

> Which of the three variables is weakest in your real project right now, and why?
