flowchart TB
    subgraph CTX["Context — a space and a source of truth, lives in the repository"]
        A["As-Is<br/>how the system is built right now"] -->|"prompt — a corridor of decisions for one turn"| B["To-Be<br/>what it should become"]
    end
