# Product Tasks

1. Create the Spring Boot 3 root project with Java 21 and Gradle Groovy DSL.
2. Model audit events, actions, outcomes, and append/read repository ports.
3. Implement append and query application services.
4. Implement JPA persistence adapters and Flyway migrations.
5. Expose append and query HTTP controllers.
6. Add Testcontainers integration tests for append, query, validation, and database immutability.
7. Document the append-only invariant and query API contract.

